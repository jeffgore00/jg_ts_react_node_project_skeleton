/* test that it's called with the correct args, i.e. git rev parse.
Mock the Date() to show expected output */

describe('hi', () => {
  it('fails', () => {
    throw new Error('write this test');
  });
});
